# RealtimeDetectionMVP

This is the canonical iOS app project directory.

Use this folder when you want to:

- open the app in Xcode
- edit the running iPhone app
- change Swift source used by the current build
- bundle `detector.pte` into the app target

Main project:

- `RealtimeDetectionMVP.xcodeproj`

Main source folder:

- `RealtimeDetectionMVP/`

Related tooling and export docs still live in:

- `../ios_mvp/`
